import React from "react";
import { Element } from "react-scroll";

const SolutionSection = () => {
  return (
    <Element
      name="solution"
      className="bg-opacity-20 h-[calc(100vh-64px)] relative flex flex-col justify-center items-center bg-red-500"
    >
      SolutionSection
    </Element>
  );
};

export default SolutionSection;
